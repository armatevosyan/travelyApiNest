import { Repository } from 'typeorm';
import { Place } from './place.entity';
import { PlaceQueryDto, UpdatePlaceDto } from './place.dto';
import { I18nService } from 'nestjs-i18n';
import { CreatePlaceData } from '@/modules/places/place.types';
import { Category } from '@/modules/categories/category.entity';
import { LocationService } from '@/modules/locations/location.service';
import { TagService } from '@/modules/tags/tag.service';
import { FilesService } from '@/modules/files/files.service';
import { FacilityService } from '@/modules/facilities/facility.service';
import { RestaurantService } from '@/modules/restaurants/restaurant.service';
import { AccommodationService } from '@/modules/accommodations/accommodation.service';
import { ShoppingService } from '@/modules/shopping/shopping.service';
import { TransportService } from '@/modules/transport/transport.service';
import { HealthWellnessService } from '@/modules/health-wellness/health-wellness.service';
import { NatureOutdoorsService } from '@/modules/nature-outdoors/nature-outdoors.service';
import { EntertainmentService } from '@/modules/entertainment/entertainment.service';
export declare class PlaceService {
    private readonly placeRepository;
    private readonly categoryRepository;
    private readonly i18n;
    private readonly locationService;
    private readonly tagService;
    private readonly filesService;
    private readonly facilityService;
    private readonly restaurantService;
    private readonly accommodationService;
    private readonly shoppingService;
    private readonly transportService;
    private readonly healthWellnessService;
    private readonly natureOutdoorsService;
    private readonly entertainmentService;
    constructor(placeRepository: Repository<Place>, categoryRepository: Repository<Category>, i18n: I18nService, locationService: LocationService, tagService: TagService, filesService: FilesService, facilityService: FacilityService, restaurantService: RestaurantService, accommodationService: AccommodationService, shoppingService: ShoppingService, transportService: TransportService, healthWellnessService: HealthWellnessService, natureOutdoorsService: NatureOutdoorsService, entertainmentService: EntertainmentService);
    private filterRelationFields;
    private filterPlacesRelations;
    create(userId: number, data: CreatePlaceData): Promise<Place>;
    findAll(query: PlaceQueryDto): Promise<{
        places: Place[];
        total: number;
    }>;
    findOne(id: number): Promise<Place>;
    findBySlug(slug: string): Promise<Place>;
    update(id: number, userId: number, updatePlaceDto: UpdatePlaceDto): Promise<Place>;
    remove(id: number, userId: number): Promise<void>;
    findByUser(userId: number): Promise<Place[]>;
    findByCategory(categoryId: number): Promise<Place[]>;
    private buildCategoryMap;
    private createCategorySpecificRecord;
    private updateCategorySpecificRecord;
    private generateSlug;
}
